name: router
description: A new brick created with the Mason CLI.
version: 0.1.0+1
vars:
  name:
    type: string
    description: The router name
    default: TheRouter
    prompt: What is the router name?
